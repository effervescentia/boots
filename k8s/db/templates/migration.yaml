apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ include "boots-db.fullname" . }}-migration"
  labels:
    {{- include "boots-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "1"
spec:
  template:
    metadata:
      labels:
        {{- include "boots-db.labels" . | nindent 8 }}
    spec:
      containers:
      - name: "{{ .Chart.Name }}-migration"
        image: "{{ .Values.migration.image.repository }}:{{ .Values.migration.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.migration.image.pullPolicy }}
        envFrom:
          - secretRef:
              name: "{{ include "boots-db.fullname" . }}-migration-secrets"
          - configMapRef:
              name: "{{ include "boots-db.fullname" . }}-migration-config"
      restartPolicy: Never
