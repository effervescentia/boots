apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.postgres.auth.existingSecret }}
  labels:
    {{- include "boots-db.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: "ref+sops://k8s/secrets.enc.yaml#/POSTGRES_PASSWORD"
  postgres-password: "ref+sops://k8s/secrets.enc.yaml#/POSTGRES_ADMIN_PASSWORD"
  replication-password: "ref+sops://k8s/secrets.enc.yaml#/POSTGRES_REPLICATION_PASSWORD"
