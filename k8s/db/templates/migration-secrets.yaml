apiVersion: v1
kind: Secret
metadata:
  name: "{{ include "boots-db.fullname" . }}-migration-secrets"
  labels:
    {{- include "boots-db.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: "ref+sops://k8s/secrets.enc.yaml#/POSTGRES_ADMIN_PASSWORD"
