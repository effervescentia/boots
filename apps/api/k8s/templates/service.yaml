apiVersion: v1
kind: Service
metadata:
  name: {{ include "boots-api.fullname" . }}
  labels:
    {{- include "boots-api.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: server
    {{- include "boots-api.selectorLabels" . | nindent 4 }}
