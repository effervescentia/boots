apiVersion: v1
kind: Secret
type: kubernetes.io/tls
metadata:
  name: "{{ include "boots-web.fullname" . }}-tls"
  namespace: {{ .Values.tls.namespace }}
  labels:
    {{- include "boots-web.labels" . | nindent 4 }}
data:
  tls.crt: "ref+sops://k8s/secrets.enc.yaml#/WEB_TLS_CERT"
  tls.key: "ref+sops://k8s/secrets.enc.yaml#/WEB_TLS_KEY"
